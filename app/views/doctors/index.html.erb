<div class="p-2">
  <h3 class="text-2xl font-bold">Doctors</h3>
  <% @doctors.each do |doctor| %>
    <p>
      <%= link_to doctor.name, doctor_path(doctor), class: 'text-blue-600 visited:text-purple-600' %>
    </p>
  <% end %>
</div>

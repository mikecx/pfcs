<div class="p-2">
  <h3 class="text-2xl font-bold">Patients</h3>
  <% @patients.each do |patient| %>
    <p>
      <%= link_to patient.name, patient_path(patient), class: 'text-blue-600 visited:text-purple-600 ' %>
    </p>
  <% end %>
</div>
